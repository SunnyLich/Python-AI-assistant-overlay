# Legacy Plugins

This folder is kept only for historical examples from the old in-process plugin
system.

New extensions should live under `addons/` and include an `addon.toml` manifest.
See [../addons/README.md](../addons/README.md) and the reference
`addons/healthcheck` addon.
